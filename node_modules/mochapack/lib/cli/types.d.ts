import { Configuration } from 'webpack';
export declare type WebpackConfigMode = 'production' | 'development' | 'none';
export declare type WebpackConfig = Configuration | ((...args: any[]) => Promise<Configuration>);
export declare type ModuleDescriptor = string | string[] | {
    module: string;
    register: Function;
};
