import { Configuration } from 'webpack';
import { WebpackConfigMode } from '../../../types';
/**
 * Requires in the user's Webpack config
 */
declare const requireWebpackConfig: (webpackConfig: string, required?: boolean, env?: string, mode?: WebpackConfigMode) => Promise<Configuration>;
export default requireWebpackConfig;
