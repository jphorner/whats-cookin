import { Options } from 'yargs';
export declare const mochapackDefaults: {
    interactive: boolean;
    'clear-terminal': boolean;
};
declare const mochapackOptions: {
    [key: string]: Options;
};
export default mochapackOptions;
